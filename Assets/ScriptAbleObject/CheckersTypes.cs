using UnityEngine;

// Enum for different types of checkers
public enum CheckersTypes
{
    None = 0,
    Slower,
    Normal,
    Faster,
}

// Enum for different sizes of checkers
public enum CheckerSize
{
    None = 0,
    Smaller,
    Normal,
    Bigger,
}